// You can add JavaScript code here if needed
// For example, handling form submission or adding more interactivity
document.addEventListener('DOMContentLoaded', function () {
    const loginForm = document.getElementById('login-form');

    // Add a class to trigger the animation
    loginForm.classList.add('animated');

    // You can customize the animation duration and easing function
    loginForm.style.animation = 'slideIn 1s ease-in-out';
});
